{
  "name": "lexi-e2e",
  "private": true,
  "description": "Playwright end-to-end tests for the lexi web UI",
  "scripts": {
    "test": "playwright test",
    "install-browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.0"
  }
}
